{
    "command":  {
                    "subir":  {
                                    "agent":  "build",
                                    "description":  "Hace git add commit push 100 por ciento fiel en el repo unico",
                                    "template":  "Sube el 100 por ciento de los cambios SIN omitir archivos. Esta carpeta ES el repo (repo unico, sin copia Fenomeno-Pagina). Ejecuta exactamente estos pasos con bash, sin preguntar:\n\n1. Muestra estado y diff en esta carpeta:\n```\ngit status\ngit diff --stat\ngit diff\n```\n\n2. Si hay cambios, haz git add ., genera mini descripcion (1 linea, estilo Avances.txt) basada en el diff real y haz:\n```\ngit add .\ngit commit -m \"\u003cmini descripcion generada\u003e\"\ngit push origin main\n```\n\n3. Verifica con git log --oneline -3 y git status que quedo limpio y pusheado a origin/main."
                                },
                    "bajar":  {
                                    "agent":  "build",
                                    "description":  "Trae cambios de GitHub a local con git pull y avisa si falla",
                                    "template":  "Trae los cambios del remoto a local (esta carpeta ES el repo). Ejecuta exactamente estos pasos con bash, sin preguntar:\n\n1. Verifica repo y remoto:\n```\ngit status\ngit remote -v\n```\n\n2. Intenta traer cambios:\n```\ngit pull origin main; if ($?) { Write-Output \"OK: local al dia con origin/main\"; git log --oneline -3; git status } else { Write-Output \"FALLO git pull - verifica: 1) internet conectado, 2) git remote -v debe mostrar https://github.com/miguelhurtado3e/sentidomatematico.git, 3) si hay conflicto haz git status y git stash o git pull --rebase\"; exit 1 }\n```"
                                }
                 },
    "$schema":  "https://opencode.ai/config.json"
}
